name: Main Branch Changes

on:
  push:
    branches:
      - main

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Local Script
        run: |
          curl -X POST ${{ secrets.WEBHOOK_URL }} -H "Content-Type: application/json" -d '{}'